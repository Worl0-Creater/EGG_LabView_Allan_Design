# 2026-5-28 进度
## 保存策略的考虑
TDMS 保存，TDMS读取 波形回放
已实现
现目前有两种结果保存路径：csv和tdms
下一步准备完善保存策略，
下一步准备完善保存策略，目前有两类思路：
一类是单功能并行按钮，一类是复用功能按钮：

具体可展开成这样子
1.如果是单功能，做成两个同等级的保存按钮，一个保存csv一个保存tdms；
2.一种单功能是 做成一个按钮是”另存为“，然后可选择格式；

还是另一种设计思想，默认计算后自动保存csv，做成一个按钮，可以另存为tdms？
目前我准备做成这样
默认计算后自动保存tdms，然后保存结果 做成一个复用按钮，可以另存为csv，后续可拓展其他格式
tdms 机器可读，快速，文件小，  csv文件大，但可读性强，需要的时候按键可另存为csv
然后回放曲线 支持是两种并行的路径输入，一种是csv一种tdms ，目前不准备做判断逻辑，先做第二版最小工程闭环


## 屏幕显示和 保存按钮的 决策prompt
labview我几乎全部依赖于gpt的对话指导，我没有知识经验，labview可以做成那种点一下按钮，弹出一个对话框，选择选项吗？这样就可以做成复用按钮， 运行时，像是操作专业软件一样，可以点一下”保存另存为“，可以指定路径和格式，也可以 点一下”回放“，可以指定路径，选择文件，可以是csv也可以tdms，然后自动再屏幕上加载。 另外屏幕是做出嗯现在的三屏幕分布（”时间误差“-”allan曲线“-”allan-回放曲线“）还是做成复用屏幕，一个大大的屏幕覆盖前面板，按钮分布在四周，屏幕可切换显示内容？先回答可行性，然后分析利弊，我来决策方向

### 我的决策：
1. 保存策略：
    只有一个“另存为”按钮，
   过程是 开始计算
→ 自动保存 TDMS
点击“另存为/导出csv”
→ 导出 CSV 或其他格式

2. 回放策略：
    两个并行按钮： TDMS结果回放
CSV复核回放
3. 屏幕显示策略： 
    一个大屏幕，内容可切换；
    大图表区域
    按钮选择：
    时间误差
    Allan曲线
    回放曲线